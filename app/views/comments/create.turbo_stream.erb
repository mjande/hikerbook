<%= turbo_stream.append "#{dom_id(@post)}_comments", @comment %>
<%= turbo_stream.update "#{dom_id(@post)}_#{dom_id(Comment.new)}" do %>
  <%= link_to 'Leave a comment', new_post_comment_path(@post) %>
<% end %>