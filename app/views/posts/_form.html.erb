<% if post.errors.any? %>
  <% post.errors.full_messages.each do |msg| %>
    <%= msg %>
  <% end %>
<% end %>

<%= form_with model: post do |f| %>
  <%= f.label 'Say something about your hike...', :body %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end %>